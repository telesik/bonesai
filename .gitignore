# Локальные настройки Claude Code — у каждого свои
.claude/settings.local.json

# macOS
.DS_Store

# Сборка PDF
build/

# Внутренние документы — намеренно не в репозитории.
# IP.md содержит персональные и юридические сведения, CLAUDE.md — рабочие инструкции.
# Оба лежат локально; резервная копия — вне git.
IP.md
CLAUDE.md
